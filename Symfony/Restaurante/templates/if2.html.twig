<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>if</title>
</head>
<body>
    {% if resultado == false %}
    No se puede calcular el factorial de {{ numero }}
    {% else %}
    El factorial de {{ numero }} es {{ resultado }}
    {% endif %}
</body>
</html>