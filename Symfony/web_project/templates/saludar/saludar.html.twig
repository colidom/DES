{% extends 'base.html.twig' %} {% block title %}{{ title }}{% endblock %} {% block body %}
<section>
    {% if mensaje is defined %}
    <p>{{ mensaje }}</p>
    {% if mostrarBotonVolver %}
    <a href="{{ path('saludar') }}" class="btn btn-primary">Volver</a>
    {% endif %} {% endif %} {% if mostrarFormulario %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% endif %}
</section>
{% endblock %}
